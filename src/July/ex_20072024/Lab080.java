package July.ex_20072024;

public class Lab080 {
    public static void main(String[] args) {

        int a = 10;
       switch (a) {  // It is a correct code

       }

        char ch = 'A'; // 65
        switch (ch) { // It is taking character because every character is a integer because every
            // character has some value (As per ASCII)
        }

      /** boolean b = true;
       switch (b) { // It won't take boolean value because it is not supported
       } **/

        long a11 = 30l;
        switch ((int) a11) { // Long is supported
        }
    }
}
